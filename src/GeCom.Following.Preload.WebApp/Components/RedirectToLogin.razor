@inject NavigationManager Navigation
@code {
    protected override void OnInitialized()
    {
        // OIDC middleware will automatically redirect to IdentityServer
        // when accessing a protected resource without authentication
        // This component just shows a loading message while the redirect happens
    }
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6 text-center">
            <div class="spinner-border text-primary" role="status">
                <span class="visually-hidden">Redirigiendo...</span>
            </div>
            <p class="mt-3">Redirigiendo al servidor de autenticación...</p>
        </div>
    </div>
</div>

