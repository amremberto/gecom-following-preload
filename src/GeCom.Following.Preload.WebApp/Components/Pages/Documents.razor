@page "/documents"
@rendermode InteractiveServer
@attribute [Microsoft.AspNetCore.Authorization.Authorize]

<PageTitle>Documents</PageTitle>

<Topbar title="Documentos" />

@if (_isLoading)
{
    <div class="text-center py-2">
        <button class="btn btn-primary" type="button" disabled>
            <span class="spinner-border spinner-border-sm me-1" role="status"
                  aria-hidden="true"></span>
            Cargando datos...
        </button>
    </div>
}

<!-- Dates and Providers filters -->
<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-header">
                <h4 class="card-title mb-0">Filtrar por fecha de emisión y proveedor</h4>
            </div><!-- end card header -->
            <div class="card-body">
                <div class="row gy-3 align-items-end">
                    <div class="col-lg-2">
                        <div>
                            <label class="form-label mb-0">Fecha Desde</label>
                            <input id="dateFrom"
                                   type="text"
                                   class="form-control"
                                   data-provider="flatpickr"
                                   data-date-format="d/m/Y"
                                   data-allow-input="true"
                                   @bind="_dateFrom">
                            <div class="invalid-feedback">
                                Ingrese una fecha válida.
                            </div>
                        </div>
                    </div>
                    <div class="col-lg-2">
                        <div>
                            <label class="form-label mb-0">Fecha Hasta</label>
                            <input id="dateTo"
                                   type="text"
                                   class="form-control"
                                   data-provider="flatpickr"
                                   data-date-format="d/m/Y"
                                   data-allow-input="true"
                                   @bind="_dateTo">
                            <div class="invalid-feedback">
                                Ingrese una fecha válida.
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div> <!-- end row-->
